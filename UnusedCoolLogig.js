//! Widdle down children of loaded component into React children only

/*let test = [];
    if (runComponent?.props?.children) {
      let childrenArray = [];
      if (!Array.isArray(runComponent.props.children)) {
        childrenArray = [runComponent.props.children];
      } else {
        childrenArray = runComponent.props.children;
      }

      childrenArray = childrenArray.filter((item) => {
        if (item?.type?.name) return true;
        return false;
      });
      console.log(childrenArray);
      test = childrenArray;
    }*/
